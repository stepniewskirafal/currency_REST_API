# Dockerfile for Java Spring Boot Application

# Step 1: Use an official Java runtime as a parent image
FROM openjdk:21-jre-slim

# Step 2: Set the working directory in the container
WORKDIR /app

# Step 3: Copy the jar file of your application to the container
COPY target/demo-0.0.1-SNAPSHOT.jar /app/demo-application.jar

# Step 4: Expose the application's port
EXPOSE 8080

# Step 5: Define the command to run your application
ENTRYPOINT ["java", "-jar", "demo-application.jar"]
